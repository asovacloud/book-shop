extends layouts/main-layout.pug

block content
  main
      form.add-product-form(action="/admin/add-product", method="POST")
        input(type="text", name="title", placeholder="Type a Product...")
        button.btn(type="submit") Add Product
